public class Solution {
    public void ReverseString(char[] s) {
         int x = s.Length - 1;

 for (int i = 0; i < s.Length; i++)
 {
     if (i<x)
     {
         char temp = s[i];
         s[i] = s[x];
         s[x] = temp;
         x--;
     }
       }
    }
}
