public class Solution {
    public int MajorityElement(int[] nums) {
         

var mostFrequent = nums.GroupBy(x => x).OrderByDescending(g => g.Count()).First();

        

        return mostFrequent.Key;
}}
